# PyPI Server Configuration
# This file contains configuration for the PyPI server

# Authentication settings
AUTHENTICATE=.
PASSWORDS=/data/packages/.htpasswd

# Package directory
PACKAGES_DIRECTORY=/data/packages

# Allow overwriting existing packages
OVERWRITE=true

# Welcome message
WELCOME_HTML=<h1>DevPod PyPI Server</h1><p>This is your private Python package repository.</p>
